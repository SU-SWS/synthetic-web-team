{
  "name": "@su-sws/synthetic-web-team",
  "version": "0.1.0",
  "description": "Stanford Web Services standards and agent team: policy, patterns, recipes, fragments, prior art, 30 skills, the two-part install wizard, and the advisory compliance CLI.",
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SU-SWS/synthetic-web-team.git"
  },
  "scripts": {
    "sync-skills": "node scripts/sync-skills.mjs",
    "validate": "node scripts/validate-skills.mjs && node scripts/validate-emit.mjs && node scripts/validate-criteria.mjs && node scripts/validate-state-classifier.mjs",
    "check": "npm run validate && npm run sync-skills -- --check",
    "pack:all": "mkdir -p .pack && npm pack --pack-destination ./.pack && npm pack -w @su-sws/synthetic-web-team-mcp --pack-destination ./.pack"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    ".",
    "packages/*"
  ],
  "devDependencies": {
    "yaml": "^2.8.1"
  },
  "bin": {
    "synthetic-web-team": "packages/wizard/bin/wizard.mjs",
    "sws": "packages/cli/bin/sws.mjs"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "AGENTS.md",
    "standards",
    "skills",
    "packages/cli/bin",
    "packages/cli/src",
    "packages/wizard/bin",
    "packages/wizard/src"
  ],
  "dependencies": {
    "node-html-parser": "^7.0.1",
    "yaml": "^2.8.1"
  },
  "peerDependencies": {
    "@playwright/test": "*",
    "@axe-core/playwright": "*"
  },
  "peerDependenciesMeta": {
    "@playwright/test": {
      "optional": true
    },
    "@axe-core/playwright": {
      "optional": true
    }
  }
}
